import React, {FC} from "react";
import Layout from "../components/ui/Layout";

const Register: FC = () => {
	return (
		<Layout>
			{/*<RegisterForm />*/}
			Register
		</Layout>
	);
};

export default Register;
